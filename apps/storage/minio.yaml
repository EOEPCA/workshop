---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: minio
  namespace: eoepca-object-storage
spec:
  interval: 2m
  url: https://charts.bitnami.com/bitnami
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minio
  namespace: eoepca-object-storage
spec:
  interval: 5m
  chart:
    spec:
      chart: minio
      sourceRef:
        kind: HelmRepository
        name: minio
        namespace: eoepca-object-storage
  values:
    auth:
      existingSecret: minio-credentials

    persistence:
      storageClass: managed-nfs-storage-retain

    ingress:
      enabled: true
      hostname: console.minio.workshop.eoepca.org
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
      tls:
        - secretName: minio-console-tls
          hosts:
            - console.minio.workshop.eoepca.org

    apiIngress:
      enabled: true
      hostname: minio.workshop.eoepca.org
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
      tls:
        - secretName: minio-tls
          hosts:
            - minio.workshop.eoepca.org

    defaultBuckets: "eoepca, cache-bucket"